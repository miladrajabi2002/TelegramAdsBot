# TelegramAdsBot — Catalog + Order-Step-3 Patches

این پچ‌ها دو بخش اصلی را بهبود می‌بخشند:

1. **پنل ادمین → کانال‌های پیشنهادی و دسته‌بندی‌ها**
2. **ثبت سفارش → مرحله ۳ از ۶ (کانال‌های هدف)**

---

## فهرست فایل‌های تغییر یافته

| # | مسیر فایل | توضیح تغییر |
|---|-----------|-------------|
| 1 | `app/Http/Controllers/Admin/CatalogController.php` | اضافه‌شدن `lookupChannel` (GET info از تلگرام)، `reorderCategories` (درگ اند دراپ)، ساده‌سازی `storeCategory`/`updateCategory` (حذف slug/description/icon از ولیدیشن و autogenerated slug)، ساده‌سازی `storeChannel`/`update` (فقط username الزامی، title/members/avatar خودکار از تلگرام، حذف `is_featured`) |
| 2 | `app/Models/TargetCategory.php` | اضافه‌شدن accessor `title` که `title_fa` را برمی‌گرداند (چون UI دیگر دو عنوان جداگانه نمی‌گیرد) |
| 3 | `app/Services/Telegram/TelegramBotClient.php` | اضافه‌شدن متد `getChatMemberCount()` که تعداد واقعی عضو کانال را از Bot API می‌گیرد |
| 4 | `routes/web.php` | اضافه‌شدن دو روت جدید: `POST /admin/channels/lookup` و `POST /admin/channel-categories/reorder` |
| 5 | `resources/views/admin/channels/index.blade.php` | بازنویسی کامل: فرم دسته ساده‌شده (فقط title + فعال)، لیست دسته‌ها با drag handle، فرم کانال با username-only + دکمه «دریافت اطلاعات» + preview، dropdown تک‌انتخابی دسته، حذف checkbox «فهرست ویژه» |
| 6 | `resources/views/admin/channels/edit.blade.php` | حذف `is_featured` checkbox، اضافه‌شدن دکمه «دریافت از تلگرام» برای refresh خودکار title/members/avatar |
| 7 | `resources/views/app/campaigns/create.blade.php` | مرحله ۳ از ۶: placeholder فارسی برای فیلد جستجو، طراحی مجدد channel-list به‌صورت کارت‌های موبایل‌محور با آواتار دایره‌ای، عنوان، @یوزرنیم، تعداد عضو، badge زبان، checkmark انیمیشن‌دار |
| 8 | `resources/css/app.css` | اضافه‌شدن استایل‌های `.channel-card` و زیرکلاس‌ها (mobile-first، Telegram-like)، استایل drag handle و حالت‌های dragging/drop-target |

---

## نحوه نصب

1. **بکاپ بگیرید** — قبل از اعمال، از فایل‌های فعلی پروژه‌تان بکاپ بگیرید.
2. فایل‌های این پچ را با حفظ ساختار مسیر در پروژه‌تان کپی کنید (overwrite).
3. **نیازی به migration نیست** — ستون‌های DB (slug، description_fa/en، icon، is_featured) سرجایشان می‌مانند ولی در UI پنهان شدند تا داده‌های قدیمی از بین نروند.
4. کش‌ها را پاک کنید:
   ```bash
   php artisan cache:clear
   php artisan view:clear
   php artisan route:clear
   php artisan config:clear
   ```
5. اگر از Vite استفاده می‌کنید، assetها را rebuild کنید:
   ```bash
   npm run build
   # یا در حالت dev:
   npm run dev
   ```

---

## توضیحات تکمیلی

### ۱. پنل ادمین → افزودن کانال
- فقط یوزرنیم را وارد کنید (با @ یا بدون @، لینک t.me، یا آیدی عددی -100...).
- دکمه «دریافت اطلاعات» را بزنید تا از تلگرام (یا از کاتالوگ محلی) اطلاعات زیر به‌صورت خودکار پر شود:
  - عنوان کانال
  - تعداد عضو (از `getChatMemberCount`)
  - عکس کانال (از `getChat` + `getFile`)
  - زبان (پیش‌فرض fa)
  - telegram_chat_id
- اگر تلگرام در دسترس نباشد یا کانال خصوصی باشد، فیلدها خالی می‌مانند و می‌توانید دستی پر کنید.
- اگر بدون زدن دکمه هم مستقیم submit کنید، سرور به‌صورت خودکار برای پر کردن فیلدهای خالی از تلگرام می‌گیرد.

### ۲. پنل ادمین → دسته‌بندی‌ها
- فرم ساخت/ویرایش فقط عنوان + تیک فعال را می‌گیرد (slug خودکار از روی title ساخته می‌شود؛ برای عنوان فارسی از یک هش پایدار استفاده می‌شود).
- توضیحات فارسی/انگلیسی و آیکون از UI حذف شدند (ستون‌ها در DB باقی می‌مانند).
- ترتیب دسته‌ها را می‌توانید با **درگ کردن** handle کنار هر دسته تغییر دهید. به‌محض رها کردن، ترتیب جدید در دیتابیس ذخیره می‌شود.

### ۳. پنل ادمین → حذف «فهرست ویژه»
- checkbox «در فهرست ویژه نمایش داده شود» از فرم افزودن و ویرایش کانال حذف شد.
- badge «پیشنهادی» از لیست کانال‌های کاربر هم حذف شد.
- ستون `is_featured` در DB باقی می‌ماند و همیشه false تنظیم می‌شود.

### ۴. ثبت سفارش → مرحله ۳ از ۶ → فیلد جستجو
- placeholder فارسی: `@یوزرنیم · یوزرنیم بدون @ · https://t.me/... · آیدی عددی -100...`
- ورودی‌های پشتیبانی‌شده:
  - `@channel_username` — strip @
  - `channel_username` (بدون @)
  - `https://t.me/channel_username`
  - `http://t.me/channel_username`
  - `-1001234567890` (آیدی عددی منفی)
  - `1234567890` (آیدی عددی مثبت با ۵+ رقم)
- بعد از Enter یا ویرگول، اطلاعات کانال (عکس، عنوان، تعداد عضو، آیدی) از کاتالوگ محلی یا از تلگرام گرفته می‌شود.

### ۵. ثبت سفارش → مرحله ۳ از ۶ → لیست کانال‌های پیشنهادی
- هر کانال به‌صورت یک کارت نمایش داده می‌شود که ظاهر و رفتار مشابه لیست کانال‌های تلگرام دارد:
  - آواتار دایره‌ای ۴۸px (یا حرف اول عنوان اگر عکس نباشد)
  - عنوان کانال
  - @یوزرنیم
  - badge با تعداد عضو + آیکون users
  - badge با کد زبان (FA/EN/AR/...)
  - checkmark انیمیشن‌دار در سمت چپ
- روی هر کارت tap کنید تا انتخاب شود؛ کل کارت tap target است (مناسب موبایل).
- در صفحات باریک (<420px)، آواتار و فونت‌ها کوچک‌تر می‌شوند تا همه چیز در صفحه جا شود.

---

## بررسی وابستگی‌ها

تغییرات با این قسمت‌های دیگر پروژه در ارتباط است ولی نیازی به تغییر آن‌ها نیست:

- `app/Http/Controllers/MiniApp/CampaignController.php` — `storeTargets()` از قبل با هر چهار فرمت ورودی (numeric ID, -chat_id, @username, t.me link) کار می‌کند؛ `searchChannel()` هم همین‌طور. تغییری نیاز نبود.
- `app/Models/SuggestedChannel.php` — fillable شامل `avatar_url`, `telegram_chat_id`, `members_count`, `is_featured` است؛ با پچ جدید هم سازگار است.
- `database/migrations/2026_08_17_000001_create_ads_platform_tables.php` — ستون‌های `slug`, `title_fa`, `title_en`, `description_fa`, `description_en`, `icon`, `sort_order`, `is_featured` همگی موجودند؛ migration جدید لازم نیست.
- `database/seeders/DatabaseSeeder.php` — هنوز slug/icon/title_fa/title_en می‌فرستد که در مدل fillable باقی می‌مانند؛ تغییری نیاز نیست (فقط در محیط local فعال است).
- `app/Providers/AppServiceProvider.php` — throttle مربوط به `miniapp-channel-search` سرجایش؛ برای endpoint ادمین throttle جداگانه‌ای اضافه نشد چون ادمین trusted است.

---

## نکات ایمنی

- **بکاپ بگیرید**: قبل از اعمال پچ، حتماً از فایل‌های `app/Http/Controllers/Admin/CatalogController.php` و `resources/views/admin/channels/*.blade.php` نسخه پشتیبان بگیرید.
- **بدون migration**: این پچ هیچ migration جدیدی اضافه نمی‌کند و هیچ ستونی از DB را حذف نمی‌کند، پس داده‌های موجود در امان است.
- **بدون ویرایش seeder**: seeder محلی هنوز داده‌های demo می‌سازد و با مدل جدید هم سازگار است.

اگر سوالی بود یا بخشی کار نکرد، محتویات فایل `storage/logs/laravel.log` را بررسی کنید — تمام خطاهای Telegram API در آن لاگ می‌شوند.
